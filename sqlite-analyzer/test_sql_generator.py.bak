import sys\nimport os\ncurrent_dir = os.path.dirname(os.path.abspath(__file__))\nif current_dir not in sys.path: sys.path.insert(0, current_dir)\nfrom src.sql_generator import SQLGenerator\ngen = SQLGenerator(['PATI_PATIENTS'], {'PATI_PATIENTS': ['PATI_ID']})\nresult, params = gen.generate_sql({'tables': ['PATI_PATIENTS'], 'conditions': {'column': 'PATI_ID', 'operator': '=', 'value': '123'}})\nprint(f'Consulta SQL: {result}')\nprint(f'Parámetros: {params}')
